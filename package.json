{
  "name": "design-token-exporter",
  "version": "1.0.0",
  "description": "Automated design token processing and deployment for iOS and Android platforms",
  "main": "index.js",
  "scripts": {
    "build": "style-dictionary build",
    "build:tokens": "node scripts/build-tokens.js",
    "clean": "rimraf build/",
    "test": "node scripts/test-setup.js",
    "test:build": "npm run clean && npm run build:tokens",
    "deploy": "npm run build:tokens && node scripts/deploy.js all",
    "deploy:ios": "npm run build:tokens && node scripts/deploy.js ios",
    "deploy:android": "npm run build:tokens && node scripts/deploy.js android"
  },
  "keywords": [
    "design-tokens",
    "style-dictionary",
    "figma",
    "ios",
    "android",
    "automation"
  ],
  "dependencies": {
    "style-dictionary": "^4.0.1"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}